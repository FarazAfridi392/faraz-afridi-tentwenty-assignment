This project uses an `.env` file to store sensitive keys like the IMDB API key. 

### Setup

1. Create a `.env` file in the root of the project:
2. Add your IMDB API key:

TMDB_API_KEY=your_api_key_here